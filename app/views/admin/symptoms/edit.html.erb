<div class="container">
  <div class="card">
    <div class="card-header">
      症状編集
    </div>
    <div class="card-body">
      <%= render 'form', symptom: @symptom %>
      <%= button_to "削除", admin_symptom_path(@symptom), method: :delete %>
    </div>
  </div>
</div>
